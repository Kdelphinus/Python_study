# 커피숍
menu = {"커피", "주스", "우유"}
print(menu, type(menu))

menu = list(menu)
print(menu, type(menu)) #[]

menu = tuple(menu)
print(menu, type(menu)) # ()

menu = set(menu)
print(menu, type(menu)) # set은 {}
